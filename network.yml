---
 - hosts: freebsd_common
   roles:
    - freebsd_common

 - hosts: gateway-a
   roles:
    - gateway-a

 - hosts: gateway-b
   roles:
    - gateway-b

 - hosts: dhcp-servers
   roles:
    - dhcpd

 - hosts: name-servers
   roles:
    - named
